{
  "$schema": "https://json.schemastore.org/package.json",
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@turbo/gen": "^2.5.6",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "husky": "^9.1.7",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.6",
    "typescript": "catalog:",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.18.1",
    "pnpm": "^10.2.1"
  },
  "name": "acme",
  "packageManager": "pnpm@10.17.0",
  "pnpm": {
    "ignoredBuiltDependencies": [
      "rolldown"
    ],
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "@ffprobe-installer/linux-x64",
      "@parcel/watcher",
      "@swc/core",
      "core-js-pure",
      "esbuild",
      "protobufjs",
      "sharp"
    ],
    "overrides": {
      "vite": "catalog:"
    }
  },
  "private": true,
  "scripts": {
    "back:dev": "docker-compose -f ./docker/docker-compose.yml up -d acme-mailhog acme-postgres acme-redis acme-drizzle acme-minio && pnpm run --filter=@acme/server start:dev",
    "bench": "turbo bench",
    "build": "turbo build",
    "clean": "rimraf dist .cache .turbo node_modules .next .velite",
    "clean:git": "git clean -xdf .cache .turbo node_modules dist",
    "docker:down": "docker-compose -f ./docker/docker-compose.yml down",
    "docker:up": "docker-compose -f ./docker/docker-compose.yml up -d",
    "format": "biome format --write ./ && biome check --write .",
    "front:dev": "docker-compose -f ./docker/docker-compose.yml up -d && pnpm run --filter=@acme/client dev",
    "lint": "biome lint --write ./",
    "lint:ws": "pnpm dlx sherif@latest",
    "postinstall": "pnpm run lint:ws",
    "prepare": "husky",
    "test": "trubo test"
  },
  "version": "1.0.0"
}
